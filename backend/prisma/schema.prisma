// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model LegalFact {
  id                 String   @id              // STARE POLE - będzie usunięte w drugiej migracji
  compositeKey       String   @default("")     // Format: "${sourceId}:${docId}:${date}"
  sourceId           String   @default("unknown") // 'eli-sejm-du', 'rss-zus', 'nfz'
  docId              String   @default("")     // Unikalny ID dokumentu per źródło
  ingestMethod       String                    // 'eli', 'rss', 'scraper'
  
  eliUri             String?
  title              String
  summary            String
  date               String
  impact             String
  category           String
  legalStatus        String
  officialRationale  String
  sourceUrl          String
  
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@unique([compositeKey])
  @@index([date])
  @@index([ingestMethod])
  @@index([sourceId])
  @@index([impact])
}
